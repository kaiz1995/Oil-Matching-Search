# 润美查询工具 — 项目记录

> 最后更新: 2026-06-12（v1.1.5）

## 项目概述

润滑油（润美）产品匹配查询工具，支持竞品匹配搜索 + 润美产品目录浏览。

## 文件位置

- **构建脚本:** `D:\HuaweiMoveData\Users\张大脸小太阳\Documents\cc workspace\润滑产品查询工具\build_all.py`
- **Excel 数据源:** `竞品产品对照表_已填充.xlsx`
  - Sheet `版本更新记录`: 版本号/更新说明/是否强制更新（v1.1.4 最新）
- **输出 data.json:** .../data.json → 自动部署到 GitHub + Android
- **输出 HTML:** .../RunmeiMatching-1.1.3.html → 自动部署到 Android（复制为 index.html）
- **GitHub Pages:** `kaiz1995/Oil-Matching-Search/data.json`
- **Android 项目:** `C:\Users\张大脸小太阳\AndroidStudioProjects\RunmeiMatching\`
- **Android 核心文件:** `MainActivity.kt`（WebView 配置 + JavaScript Bridge）

## 工作流

1. **修改 Excel** → 编辑 `竞品产品对照表_已填充.xlsx`
2. **运行构建:** `python build_all.py` (在 oil-pm 目录)
3. **自动完成:** data.json 更新 → 推送到 GitHub → 复制到 Android 项目

## 功能状态

### 已完成
- [x] 快速搜索（全局搜索所有字段）
- [x] 竞品匹配（模糊评分，按品牌/类型/粘度筛选）
- [x] 分类浏览（按产品类型/品牌浏览）
- [x] **润美产品浏览（第4标签）** — 2026-05-21 新增
  - 从 Excel"润美产品目录"sheet 读取 95 个产品
  - 按产品系列筛选 + 关键词搜索
  - 卡片网格展示，点击弹出详情
  - 技术参数表只显示有数据的行
- [x] **TDS/MSDS 下载及理化参数三列标准自适应升级** — 2026-06-11 新增
  - 物理建立并托管 `pdfs/` 官方文件目录，成功搜集并同步测试数据（L-HM 46 的说明书和安全说明书）。
  - 构建脚本 `build_all.py` 智能解析 Excel 的 TDS 与 MSDS 文件名并拼装为 URL 编码绝对线上 URL。
  - 前端 HTML 参数表重构为三列标准（分析项目、试验方法、典型数据），按国家/行业检测标准对齐，并保持空值自适应隐藏。
  - 详情卡片绘制 PDF 文档下载区，判断有无数据自动激活下载或置灰（提示暂无文档），App 端调用 `AndroidBridge.openInBrowser` 安全分流。
  - 增加了企业 VI `@media print` 打印版式规范。
- [x] **检索体验升级与 L-HM 46 暂无匹配修复** — 2026-06-11 新增
  - 快速搜索卡片在已对齐润美产品时，新增“技术规格/TDS”一键跳转详情按钮，极大简化操作链路。
  - 实现连写多粘度型号（如 `32/46/68`）的智能粘度拆分选择。点击跳转时弹出轻量粘度选择卡（`[32] [46] [68]`），点击单项可一键检索定位具体的拆分牌号详情弹窗。
  - 重构润美 Tab 的 `filterRunmei` 检索为多词模糊分词 AND 匹配，全面提升对拼写、空格、斜杠的容错率。
  - 将 `#runmei-modal` 弹窗 DOM 层级提升至 body 根级别，彻底修复因 Tab 切换父容器隐藏导致弹窗无法显示的 Bug。
- [x] 自动部署（git push + Android assets 复制）
- [x] **修复 TDS 下载 404 及 PDF 部署管线** — 2026-06-11 新增
  - 根因：`build_all.py` 缺少 `pdfs/` 部署步骤，PDF 文件未同步到 GitHub Pages
  - 修复：新增 Step 1b 复制 `pdfs/` → GitHub repo，加入 `git add pdfs/` 随 push 部署
  - 修复 MSDS 只读文件权限问题（`os.chmod` + `shutil.copy2`）
  - 验证：L-HM 46 TDS/MSDS 两个 PDF 均已返回 200 OK（GitHub Pages 可访问）
- [x] **产品信息可视化图片（替代 TDS PDF 下载）** — 2026-06-11 新增
  - 用户不需要 PDF 文档，改为生成**产品数据卡图片**（PNG 格式）
  - 使用 `html2canvas` 在浏览器端将产品信息渲染为高清图片
  - 图片包含：产品名称、系列标签、性能特点（要点列表）、关键技术参数（视觉卡片网格）、应用信息（行业/部位/包装/备注）
  - 参数以 5 列圆角卡片网格展示，仅显示有数据的项目，排版清晰美观
  - 详情弹窗下载区改为"生成产品数据卡"为主按钮，保留 TDS/MSDS 为次要下载选项
  - 新增加载遮罩提示，生成后自动下载 PNG 文件
  - 移除 `build_all.py` 中的 TDS PDF 自动生成步骤（`generate_tds.py` 保留作为可选工具）
- [x] **动态列检测：润美产品新增列自动读取并显示** — 2026-06-11 新增
  - 原问题：`parse_runmei_sheet` 硬编码 `param_map` 指定列号，用户新增列（如 AE 列 FZG）后需手动改代码
  - `build_all.py`：重写 `parse_runmei_sheet`，自动读取 Excel row 1 所有表头，按关键字匹配映射字段名
  - 已知列（运动粘度40、FZG 等）映射为规范字段名；未知列自动生成字段名
  - `data.json` 新增 `paramConfig` 数组，描述所有参数列的键名/标签/检测标准
  - HTML 移除硬编码 `TDS_PARAM_CONFIG`，改为 `getParamConfig()` 动态读取
  - `showRunmeiDetail` 参数表和 `generateProductImageCard` 图片卡均使用动态配置
  - 后续用户在 Excel 新增列→运行 `build_all.py`→自动出现在参数表和图片卡中
- [x] **彻底解决 FZG 数据反复丢失问题** — 2026-06-12 修复
  - 根因：`checkForUpdates()` 从 GitHub Pages 拉取旧版 `data.json`（不含 23 列 `paramConfig`），直接覆盖本地已加载的参数配置 → PARAM_CONFIG 回退到 12 列硬编码 → FZG 消失
  - 修复：在所有 8 个数据加载路径（EMBEDDED_DATA / GitHub fetch / localStorage / data.json / Android bridge / checkForUpdates / 各 fallback）的参数赋值处增加保护：`if (json.paramConfig && json.paramConfig.length > 0) { PARAM_CONFIG = json.paramConfig; }`
  - 同时推送最新 `data.json`（version `2026-06-12-v1`，23 个参数含 FZG A20/8.3/90）到 GitHub Pages
  - 验证：现在无论本地/远程/缓存数据版本差异，paramConfig 都不会被旧数据覆盖
- [x] **抗乳化性重复修复 + 密度标签补全** — 2026-06-12 修复
  - 抗乳化性从单一重复行拆分为 3 个独立字段：`demulsibility_54`, `demulsibility_82`, `demulsibility_82_detail`
  - 密度标签补全为 `密度（15°C）(g/cm³)`，FZG 标签更新为 `FZG A20/8.3/90`（标准 `NB/SH/T 0306-2013`）
- [x] **全量代码审查与修复** — 2026-06-12 完成
  - 经过 2 轮审查-修复循环，发现并修复 6 个 bug + 1 项缺失标准号
  - 所有问题修复后经检查确认无残留问题
- [x] **Android 更新通知修复（v1.1.4）** — 2026-06-12 完成
  - 根因：Android WebView 从 `file://` 加载 HTML 后，`fetch()` 到 `https://` 被浏览器 CORS 策略拦截 → GitHub 数据永远获取失败 → `checkAppUpdate` 无法检测新版本
  - 修复 ①：`MainActivity.kt` 改用 `loadDataWithBaseURL("https://kaiz1995.github.io/Oil-Matching-Search/", ...)` 加载 HTML，使 WebView origin 视为 GitHub Pages，`fetch(data.json)` 变为同源请求
  - 修复 ②：`checkForUpdates()` 中不论数据版本是否变更，始终调用 `checkAppUpdate(json)` 检查 app 更新
  - 修复 ③：`downloadProductImage()` 中 Android 路径改用 `canvas.toDataURL('image/jpeg')` + `AndroidBridge.openInBrowser()`，解决手机无法保存产品数据卡图片的问题
  - 修复 ④：`build_all.py` 输出 HTML 文件名从硬编码 `1.1.2` 改为 `1.1.3`
- [x] **修复产品数据卡功能 + v1.1.5 发布** — 2026-06-12 完成
  - 根因：`openInBrowser(dataUrl)` 将 `data:` URI 通过 `Intent.ACTION_VIEW` 传递，Android 系统浏览器无法处理此类 URI → 图片无法保存
  - 修复：`MainActivity.kt` 新增 `saveBase64Image(base64Data, fileName)` JS Bridge 方法，直接在原生层解码 base64 并写入 Downloads 目录（Android 10+ 用 MediaStore，旧版用直接文件写入 + MediaScanner 通知）
  - `downloadProductImage()` 更新：优先调用 `AndroidBridge.saveBase64Image()` 保存图片，成功提示"已保存到下载目录"，失败则降级回 `openInBrowser`（兜底）
  - 版本号 v1.1.5，强制更新
  - build_all.py 同时输出 versioned + latest（RunmeiMatching.html）两份 HTML

### 待填写
- [ ] 部分产品技术参数栏位有待补充（以用户手动录入为准）

## data.json 结构

```json
{
  "version": "2026-06-12-v1",
  "updatedAt": "2026-06-12",
  "totalProducts": 326,
  "products": [...],
  "runmeiCatalog": [
    {
      "series": "风电齿轮油",
      "product_cn": "Lubemater CF-20 320 风电齿轮油",
      "packaging": "18L/200L/1000L",
      "industry": "风电",
      "application_site": "风电机组齿轮箱",
      "features": "抗磨损和抗微点蚀能力...",
      "notes": "基础油类型",
      "kv40": "151.8",
      "tds_file": "https://kaiz1995.github.io/Oil-Matching-Search/pdfs/{encoded_name}_TDS.pdf",
      ...
    }
  ],
  "paramConfig": [...],
  "appUpdate": {
    "latestVersion": "1.1.4",
    "updateNotes": "...",
    "apkUrl": "https://kaiz1995.github.io/Oil-Matching-Search/RunmeiMatching.apk",
    "forceUpdate": false
  },
  "versionHistory": [...],
  "releasePages": {...}
}
```

## 技术参数列（对应 Excel H~S 列及后续列）

| 键名 | 中文名 |
|------|--------|
| kv40 | 运动粘度40℃(mm²/s) |
| kv100 | 运动粘度100℃(mm²/s) |
| vi | 粘度指数 |
| pour_point | 倾点(℃) |
| flash_point | 闪点(℃) |
| copper_corrosion | 铜片腐蚀(100℃,3h) |
| rust_a | 液相锈蚀A法 |
| rust_b | 液相锈蚀B法 |
| cleanliness_nas | 清洁度(NAS1638) |
| cleanliness_iso | 清洁度(ISO 4406) |
| wear_scar | 磨斑直径(mm) |
| weld_load | 烧结负荷(N) |
| demulsibility_54 | 抗乳化性(54℃)(min) |
| demulsibility_82 | 抗乳化性(82℃)(min) |
| demulsibility_82_detail | 抗乳化性(82℃) 乳化液/分离水/油中水 |
| density | 密度（15°C）(g/cm³) |
| fzg | FZG A20/8.3/90 |
| solidification_point | 凝点(℃)

## 已知问题

- 无（SyntaxWarnings 是已知的，来自 JS 正则 `\s` `\d`，不影响运行）
- pdfs/ 目录在 Windows 部署时可能因某些字符编码问题导致 [SKIP]（WinError 3），不影响工具功能
